{
  "name": "whiterabbit",
  "version": "0.2.0",
  "private": true,
  "description": "Light-only PWA for accountable public-sector sensing",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=24.18.0 <27",
    "pnpm": "11.21.0"
  },
  "scripts": {
    "build": "corepack pnpm -r --if-present build",
    "dev:web": "corepack pnpm --filter @whiterabbit/web dev",
    "format": "prettier --write package.json pnpm-workspace.yaml tsconfig.base.json eslint.config.mjs apps packages",
    "format:check": "prettier --check package.json pnpm-workspace.yaml tsconfig.base.json eslint.config.mjs apps packages",
    "lint": "corepack pnpm -r --if-present lint",
    "quality:supply-chain": "python3 scripts/quality/supply_chain.py",
    "test": "corepack pnpm -r --if-present test",
    "typecheck": "corepack pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "@eslint/js": "9.39.5",
    "@types/node": "24.13.3",
    "eslint": "9.39.5",
    "prettier": "3.9.6",
    "typescript-eslint": "8.67.0",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
